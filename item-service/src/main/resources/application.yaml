server:
  port: 8081
spring:
  application:
    name: item-service
  profiles:
    active: dev
feign:
  hystrix:
    enabled: true

hm:
  db:
    database: hm-item
  swagger:
    title: 商品服务接口文档
    package: com.hmall.item.controller